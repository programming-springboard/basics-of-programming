#include <stdio.h>
#include <unistd.h>

int main() {
    int secondsLeft = 3;

    while (secondsLeft >= 0) {
        printf("It's %d seconds left ...\n", secondsLeft);
        sleep(1);
        secondsLeft--;
    }

    printf("Something has happened\n");
    return 0;
}
